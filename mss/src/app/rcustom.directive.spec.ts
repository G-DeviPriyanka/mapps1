import { RcustomDirective } from './rcustom.directive';

describe('RcustomDirective', () => {
  it('should create an instance', () => {
    const directive = new RcustomDirective();
    expect(directive).toBeTruthy();
  });
});
